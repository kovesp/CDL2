
//cspell:disable
/*
 * C# program generated by CDL2 v1.0.0 for PROGRAM csharp sort at 2025-12-22 13:32:46.
 */

using System;

#pragma warning disable CS0162 // Unreachable code detected
#pragma warning disable CS0219 // Variable is assigned but its value is never used

namespace CDL2Generated {
   public class BoundedArray<T> {
      public Int64 LowerBound { get; }
      public Int64 UpperBound { get; }
      public readonly T[] array;

      public BoundedArray(Int64 lowerBound, Int64 upperBound) {
         if (upperBound - lowerBound < 0)
            throw new ArgumentException($"Upper bound {upperBound} must be greater than or equal to lower bound {lowerBound}.");
         LowerBound = lowerBound;
         UpperBound = upperBound;
         array = new T[upperBound - lowerBound + 1];
      }

      private void CheckIndex(params Int64[] indices) {
         foreach (int index in indices) {
            if (index < LowerBound || index > UpperBound)
               throw new IndexOutOfRangeException($"Index: {index}, LowerBound: {LowerBound}, UpperBound: {UpperBound}");
         }
      }

      public T this[Int64 index] {
         get { CheckIndex(index); return array[index - LowerBound]; }
         set { CheckIndex(index); array[index - LowerBound] = value; }
      }

      public void Swap(Int64 i, Int64 j) {
         CheckIndex(i, j);
         (array[i - LowerBound], array[j - LowerBound]) = (array[j - LowerBound], array[i - LowerBound]);
      }
   }

   public static class Program {
      //===============================================================================

// Begin PROG csharp sort

// ##### 6 Consts #####

private const long ckernel_kernel_arithmetic_constx = 1;
private const long quicksort_base_parametrization_upb = 10000;
private const long quicksort_base_parametrization_lwb = 1;
private const long quicksort_base_parametrization_minrandom = 0;
private const long quicksort_base_parametrization_maxrandom = 200;
private const long quicksort_base_parametrization_maxsizeoflisttoshow = 100;

// ##### 3 Vars #####

private static long v_ckernel_kernel_data_datasize = 493606219  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
private static long v_ckernel_kernel_timer_start = 2069854292  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
private static long v_ckernel_kernel_timer_stop = 1444787294  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;

// ##### 1 LIST #####

private static BoundedArray<long> ckernel_kernel_data_data = new BoundedArray<long>(quicksort_base_parametrization_lwb, quicksort_base_parametrization_upb);

// ##### 23 Macros #####


// FUNCTION add+>a+>b+c> =
//    c " = " a " + " b ";".
private static void ckernel_kernel_arithmetic_add(long a_a, long a_b, ref long a_c) {
   a_c = a_a + a_b;
}

// FUNCTION subtract+>a+>b+c> =
//    c " = " a " - " b ";".
private static void ckernel_kernel_arithmetic_subtract(long a_a, long a_b, ref long a_c) {
   a_c = a_a - a_b;
}

// FUNCTION incr+>a> =
//    a "++;".
private static void ckernel_kernel_arithmetic_incr(ref long a_a) {
   a_a++;
}

// FUNCTION make+a>+>b =
//    a " = " b ";".
private static void ckernel_kernel_arithmetic_make(ref long a_a, long a_b) {
   a_a = a_b;
}

// TEST le+>a+>b =
//    a " <= " b.
private static bool ckernel_kernel_arithmetic_le(long a_a, long a_b) {
   return a_a <= a_b;
}

// FUNCTION random+>min+>max+r> =
//    r " = " min " + rand() % ((" max ") - (" min ") + 1);".
private static void ckernel_kernel_arithmetic_random(long a_min, long a_max, ref long a_r) {
   a_r = a_min + rand() % ((a_max) - (a_min) + 1);
}

// ACTION set element+>i+>v =
//    "CDL2_LIST_SET(" data ", " i ", " v ");".
private static void ckernel_kernel_data_setelement(long a_i, long a_v) {
   CDL2_LIST_SET(ckernel_kernel_data_data, a_i, a_v);
}

// ACTION show msg no newline*string =
//    "printf($"%s$", " string ");".
private static void ckernel_kernel_messages_showmsgnonewline(string a_string) {
   printf("%s", a_string);
}

// ACTION dump list =
//    "$Lfor (long i = " lwb "; i <= " upb "; i++)" 
//    "$L   printf($"%ld $", CDL2_LIST_GET(" data ", i));".
private static void ckernel_kernel_data_dumplist() {
   for (long i = quicksort_base_parametrization_lwb; i <= quicksort_base_parametrization_upb; i++)
      printf("%ld ", CDL2_LIST_GET(ckernel_kernel_data_data, i));
}

// ACTION show msg*string =
//    "printf($"%s\n$", " string ");".
private static void ckernel_kernel_messages_showmsg(string a_string) {
   printf("%s\n", a_string);
}

// FUNCTION touch out+v> =
//    "".
private static void quicksort_base_touch_touchout(ref long a_v) {
   }

// FUNCTION touch in+>v =
//    "".
private static void quicksort_base_touch_touchin(long a_v) {
   }

// TEST is data sorted macro -i -r =
//    r " = 1;" 
//    "$Lfor (" i " = " lwb "; " i " < " upb "; " i "++) {" 
//    "$L   if (CDL2_LIST_GET(" data ", " i ") > CDL2_LIST_GET(" data ", " i " + 1)) {" 
//    "$L      " r " = 0;" 
//    "$L      break;" 
//    "$L   }" 
//    "$L}" 
//    "$L" r " == 1".
private static bool ckernel_kernel_data_isdatasortedmacro() {
   long l_i = 1632141945  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   long l_r = 481805402  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   l_r = 1;
   for (l_i = quicksort_base_parametrization_lwb; l_i < quicksort_base_parametrization_upb; l_i++) {
      if (CDL2_LIST_GET(ckernel_kernel_data_data, l_i) > CDL2_LIST_GET(ckernel_kernel_data_data, l_i + 1)) {
         l_r = 0;
         break;
      }
   }
   return l_r == 1;
}

// ACTION show number+>number =
//    "printf($"%ld$", " number ");".
private static void ckernel_kernel_messages_shownumber(long a_number) {
   printf("%ld", a_number);
}

// ACTION start timer =
//    start " = clock();".
private static void ckernel_kernel_timer_starttimer() {
   v_ckernel_kernel_timer_start = clock();
}

// TEST lt+>a+>b =
//    a " < " b.
private static bool ckernel_kernel_arithmetic_lt(long a_a, long a_b) {
   return a_a < a_b;
}

// FUNCTION halve+>a> =
//    a " /= 2;".
private static void ckernel_kernel_arithmetic_halve(ref long a_a) {
   a_a /= 2;
}

// TEST smaller element+>p+>q =
//    "CDL2_LIST_GET(" data ", " p ") < CDL2_LIST_GET(" data ", " q ")".
private static bool ckernel_kernel_data_smallerelement(long a_p, long a_q) {
   return CDL2_LIST_GET(ckernel_kernel_data_data, a_p) < CDL2_LIST_GET(ckernel_kernel_data_data, a_q);
}

// FUNCTION decr+>a> =
//    a "--;".
private static void ckernel_kernel_arithmetic_decr(ref long a_a) {
   a_a--;
}

// ACTION swap element+>i+>j =
//    "CDL2_LIST_SWAP(" data ", " i ", " j ");".
private static void ckernel_kernel_data_swapelement(long a_i, long a_j) {
   CDL2_LIST_SWAP(ckernel_kernel_data_data, a_i, a_j);
}

// ACTION stop timer =
//    stop " = clock();".
private static void ckernel_kernel_timer_stoptimer() {
   v_ckernel_kernel_timer_stop = clock();
}

// ACTION show elapsed time =
//    "printf($"Elapsed time: %.0f milliseconds\n$", (double)(" stop " - " start 
//    ") * 1000.0 / CLOCKS_PER_SEC);".
private static void ckernel_kernel_timer_showelapsedtime() {
   printf("Elapsed time: %.0f milliseconds\n", (double)(v_ckernel_kernel_timer_stop - v_ckernel_kernel_timer_start) * 1000.0 / CLOCKS_PER_SEC);
}

// ACTION sort data =
//    "$Lint cmp(const void *a, const void *b) { return (*(long*)a - *(long*)b); }" 
//    "$Lqsort(" data "_array, " upb " - " lwb " + 1, sizeof(long), cmp);".
private static void ckernel_kernel_data_sortdata() {
   int cmp(const void *a, const void *b) { return (*(long*)a - *(long*)b); }
   qsort(ckernel_kernel_data_data_array, quicksort_base_parametrization_upb - quicksort_base_parametrization_lwb + 1, sizeof(long), cmp);
}

// ##### 12 Procedures #####


// ACTION initialize data -i -r -z :
//    add+const x+const x+z, subtract+upb+lwb+data size, incr+data size, make+i+lwb, 
//       (le+i+upb, random+min random+max random+r, set element+i+r, incr+i, *;
//        +).
private static void ckernel_kernel_data_initializedata() {
   long l_i = 1284667817  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   long l_r = 866603250  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   long l_z = 2129515129  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   // Alternative 1
   ckernel_kernel_arithmetic_add(ckernel_kernel_arithmetic_constx, ckernel_kernel_arithmetic_constx, ref l_z);
   ckernel_kernel_arithmetic_subtract(quicksort_base_parametrization_upb, quicksort_base_parametrization_lwb, ref v_ckernel_kernel_data_datasize);
   ckernel_kernel_arithmetic_incr(ref v_ckernel_kernel_data_datasize);
   ckernel_kernel_arithmetic_make(ref l_i, quicksort_base_parametrization_lwb);
   // Group
   while (true) {
      // Alternative 1
      if (ckernel_kernel_arithmetic_le(l_i, quicksort_base_parametrization_upb)) {
         ckernel_kernel_arithmetic_random(quicksort_base_parametrization_minrandom, quicksort_base_parametrization_maxrandom, ref l_r);
         ckernel_kernel_data_setelement(l_i, l_r);
         ckernel_kernel_arithmetic_incr(ref l_i);
         continue;
      }
      // End Alternative 1
      // Alternative 2
      return;
      // End Alternative 2
      break;
   }
   // End Group
   // End Alternative 1
   return;
}

// ACTION run demo -data size :
//    initialize data, dump data+"Data before sorting: ", verify data sorted+"Random", 
//       get data size+data size, show msg no newline+"Running quicksort on list of ", 
//          show number+data size, show msg+" elements.", start timer, quicksort+lwb+upb, stop timer, 
//             show elapsed time, dump data+"Data after sorting: ", verify data sorted+"Quicksort-ed", 
//             initialize data, show msg+"Running .NET sort on list.", start timer, sort data, 
//             stop timer, show elapsed time.
private static void quicksort_sort_sorter_rundemo() {
   long l_datasize = 428838740  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   // Alternative 1
   ckernel_kernel_data_initializedata();
   ckernel_kernel_data_dumpdata("Data before sorting: ");
   quicksort_sort_sorter_verifydatasorted("Random");
   ckernel_kernel_data_getdatasize(ref l_datasize);
   ckernel_kernel_messages_showmsgnonewline("Running quicksort on list of ");
   ckernel_kernel_messages_shownumber(l_datasize);
   ckernel_kernel_messages_showmsg(" elements.");
   ckernel_kernel_timer_starttimer();
   quicksort_sort_sorter_quicksort(quicksort_base_parametrization_lwb, quicksort_base_parametrization_upb);
   ckernel_kernel_timer_stoptimer();
   ckernel_kernel_timer_showelapsedtime();
   ckernel_kernel_data_dumpdata("Data after sorting: ");
   quicksort_sort_sorter_verifydatasorted("Quicksort-ed");
   ckernel_kernel_data_initializedata();
   ckernel_kernel_messages_showmsg("Running .NET sort on list.");
   ckernel_kernel_timer_starttimer();
   ckernel_kernel_data_sortdata();
   ckernel_kernel_timer_stoptimer();
   ckernel_kernel_timer_showelapsedtime();
   return;
   // End Alternative 1
   return;
}

// ACTION dump data*msg :
//    show msg no newline+msg, 
//       (le+data size+max size of list to show, dump list;
//        show msg+"too many elements to show.").
private static void ckernel_kernel_data_dumpdata(string a_msg) {
   // Alternative 1
   ckernel_kernel_messages_showmsgnonewline(a_msg);
   // Group
      // Alternative 1
      if (ckernel_kernel_arithmetic_le(v_ckernel_kernel_data_datasize, quicksort_base_parametrization_maxsizeoflisttoshow)) {
         ckernel_kernel_data_dumplist();
         return;
      }
      // End Alternative 1
      // Alternative 2
      ckernel_kernel_messages_showmsg("too many elements to show.");
      return;
      // End Alternative 2
   // End Group
   // End Alternative 1
   return;
}

// ACTION verify data sorted*which :
//    is data sorted, show msg no newline+which, show msg+" data is sorted.";
//    show msg no newline+which, show msg+" data is not sorted.".
private static void quicksort_sort_sorter_verifydatasorted(string a_which) {
   // Alternative 1
   if (quicksort_sort_sorter_isdatasorted()) {
      ckernel_kernel_messages_showmsgnonewline(a_which);
      ckernel_kernel_messages_showmsg(" data is sorted.");
      return;
   }
   // End Alternative 1
   // Alternative 2
   ckernel_kernel_messages_showmsgnonewline(a_which);
   ckernel_kernel_messages_showmsg(" data is not sorted.");
   return;
   // End Alternative 2
   return;
}

// PREDICATE is data sorted -where :
//    use macro for sortedness test, touch out+where, touch in+where, is data sorted macro;
//    is data unsorted procedure+where, show msg no newline+"Unsorted at ", show number+where, 
//       show msg+".", -;
//    +.
private static bool quicksort_sort_sorter_isdatasorted() {
   long l_where = 1089134548  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   // Alternative 1
   quicksort_base_touch_touchout(ref l_where);
   quicksort_base_touch_touchin(l_where);
   if (ckernel_kernel_data_isdatasortedmacro()) {
      return true;
   }
   // End Alternative 1
   // Alternative 2
   // Alternative suppressed by previous conditional compilation ON
   // End Alternative 2
   // Alternative 3
   // Alternative suppressed by previous conditional compilation ON
   // End Alternative 3
   return false;
}

// TEST use macro for sortedness test :
//    +.

// FUNCTION get data size+size> :
//    make+size+data size.
private static void ckernel_kernel_data_getdatasize(ref long a_size) {
   // Alternative 1
   ckernel_kernel_arithmetic_make(ref a_size, v_ckernel_kernel_data_datasize);
   return;
   // End Alternative 1
   return;
}

// ACTION quicksort+>from+>to -p -q :
//    less+from+to, split+from+to+p+q, quicksort+from+q, quicksort+p+to;
//    +.
private static void quicksort_sort_sorter_quicksort(long a_from, long a_to) {
   long l_p = 1004858958  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   long l_q = 906988780  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   // Alternative 1
   if (quicksort_base_arithmetic_less(a_from, a_to)) {
      quicksort_sort_sorter_split(a_from, a_to, ref l_p, ref l_q);
      quicksort_sort_sorter_quicksort(a_from, l_q);
      quicksort_sort_sorter_quicksort(l_p, a_to);
      return;
   }
   // End Alternative 1
   // Alternative 2
   return;
   // End Alternative 2
   return;
}

// TEST less+>a+>b :
//    lt+a+b.
private static bool quicksort_base_arithmetic_less(long a_a, long a_b) {
   // Alternative 1
   if (ckernel_kernel_arithmetic_lt(a_a, a_b)) {
      return true;
   }
   // End Alternative 1
   return false;
}

// ACTION split+>i+>j+p>+q> -m :
//    make+p+i, make+q+j, add+i+j+m, halve+m, 
//       (again:move p up+j+p+m, move q down+i+q+m, 
//          (less+p+q, swap element+p+q, incr+p, decr+q, *again;
//           less+p+m, swap element+p+m,  incr+p;
//           less+m+q, swap element+q+m,  decr+q;
//           +)).
private static void quicksort_sort_sorter_split(long a_i, long a_j, ref long a_p, ref long a_q) {
   long l_m = 2120691438  /* Random value to catch uninitialized VARs, LOCALs, and output AFFIXes */;
   // Alternative 1
   ckernel_kernel_arithmetic_make(ref a_p, a_i);
   ckernel_kernel_arithmetic_make(ref a_q, a_j);
   ckernel_kernel_arithmetic_add(a_i, a_j, ref l_m);
   ckernel_kernel_arithmetic_halve(ref l_m);
   // Group
   while (true) {
      // Alternative 1
      quicksort_sort_sorter_movepup(a_j, ref a_p, l_m);
      quicksort_sort_sorter_moveqdown(a_i, ref a_q, l_m);
      // Group
         // Alternative 1
         if (quicksort_base_arithmetic_less(a_p, a_q)) {
            ckernel_kernel_data_swapelement(a_p, a_q);
            ckernel_kernel_arithmetic_incr(ref a_p);
            ckernel_kernel_arithmetic_decr(ref a_q);
            continue;
         }
         // End Alternative 1
         // Alternative 2
         if (quicksort_base_arithmetic_less(a_p, l_m)) {
            ckernel_kernel_data_swapelement(a_p, l_m);
            ckernel_kernel_arithmetic_incr(ref a_p);
            return;
         }
         // End Alternative 2
         // Alternative 3
         if (quicksort_base_arithmetic_less(l_m, a_q)) {
            ckernel_kernel_data_swapelement(a_q, l_m);
            ckernel_kernel_arithmetic_decr(ref a_q);
            return;
         }
         // End Alternative 3
         // Alternative 4
         return;
         // End Alternative 4
      // End Group
      // End Alternative 1
      break;
   }
   // End Group
   // End Alternative 1
   return;
}

// FUNCTION move p up+>j+>p>+>m :
//    less+j+p;
//    smaller element+m+p;
//    incr+p, *.
private static void quicksort_sort_sorter_movepup(long a_j, ref long a_p, long a_m) {
   while (true) {
      // Alternative 1
      if (quicksort_base_arithmetic_less(a_j, a_p)) {
         return;
      }
      // End Alternative 1
      // Alternative 2
      if (ckernel_kernel_data_smallerelement(a_m, a_p)) {
         return;
      }
      // End Alternative 2
      // Alternative 3
      ckernel_kernel_arithmetic_incr(ref a_p);
      continue;
      // End Alternative 3
      return;
   }
   return;
}

// FUNCTION move q down+>i+>q>+>m :
//    less+q+i;
//    smaller element+q+m;
//    decr+q, *.
private static void quicksort_sort_sorter_moveqdown(long a_i, ref long a_q, long a_m) {
   while (true) {
      // Alternative 1
      if (quicksort_base_arithmetic_less(a_q, a_i)) {
         return;
      }
      // End Alternative 1
      // Alternative 2
      if (ckernel_kernel_data_smallerelement(a_q, a_m)) {
         return;
      }
      // End Alternative 2
      // Alternative 3
      ckernel_kernel_arithmetic_decr(ref a_q);
      continue;
      // End Alternative 3
      return;
   }
   return;
}

// ##### 2 Synthetic Procedures #####

// MOD c kernel LAY kernel SEC data ACTION PRELUDE
private static void ckernel_kernel_data__PRELUDE() {
   // Alternative 1
   ckernel_kernel_data_initializedata();
   return;
   // End Alternative 1
   return;
}
// MOD quicksort LAY sort SEC sorter ACTION ROOT
private static void quicksort_sort_sorter__ROOT() {
   // Alternative 1
   quicksort_sort_sorter_rundemo();
   return;
   // End Alternative 1
   return;
}

// PROGRAM csharp sort.
//    PART csharp kernel, quicksort.
//    PRELUDE csharp kernel.
//    ROOT quicksort.
// ENDPROG csharp sort.
// 
public static void Main(string[] args) {
   try {
      // Program Ludes
      // MOD_csharpkernel__PRELUDE
         csharpkernel_kernel_data__PRELUDE();
      // MOD_quicksort__ROOT
         quicksort_sort_sorter__ROOT();
   } catch (Exception ex) {
      Console.WriteLine($"Error: {ex.Message}");
      Console.WriteLine($"Stack Trace:\n{ex.StackTrace}");
   }
}
// End PROG csharp sort
   }
}
